import { MaskDecimalDirective } from './mask-decimal.directive';

describe('MaskDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
